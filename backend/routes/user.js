const express = require("express");
const router = express.Router();

// Log in route
router.post("/login", () => {})

// Sign up route
router.post("/signup", () => {})

// Delete account route
router.post("/delete", () => {})

module.exports = router